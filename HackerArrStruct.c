#include <stdio.h>
#include <stdlib.h>
#include <string.h>



int main()
{
    // remove the comment line and execute the program

    // struct student

    // {

    // int id;

    // char name[30];

    // float percentage;

    // };

    // int i;

    // struct student record[2];

    // record[0].id=1;

    // strcpy(record[0].name, "aaa");

    // record[0].percentage = 86.5;

    // record[1].id=2;

    // strcpy(record[1].name, "Sss");

    // record[1].percentage = 90.5;

    // record[2].id=3;

    // strcpy(record[2].name, "bbb");

    // record[2].percentage = 81.5;

    // for(i=0; i<3; i++)

    // {

    /*
     * printf("     Records of STUDENT : %d
     * ", i+1);
     */

    /*
     * printf(" Id is: %d
     * ", record[i].id);
     */

    /*
     * printf(" Name is: %s
     * ", record[i].name);
     */

    /*
     * printf(" Percentage is: %f
     * ",record[i].percentage);
     */

    // }

    return 0;
}
